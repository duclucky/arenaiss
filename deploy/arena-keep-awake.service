[Unit]
Description=Prevent sleep while Arena ISS is hosted

[Service]
Type=simple
ExecStart=/usr/bin/systemd-inhibit --what=sleep:idle --who=Arena-ISS --why=Hosting-Arena-ISS --mode=block /usr/bin/sleep infinity
Restart=always
RestartSec=5

[Install]
WantedBy=default.target
