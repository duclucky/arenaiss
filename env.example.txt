# Renaiss Arena — biến môi trường
# CHỈ dùng server-side (route handlers). KHÔNG prefix NEXT_PUBLIC_ cho các key.
# KHÔNG commit file .env thật; đây chỉ là mẫu.

# --- Renaiss OS Index API (partner credentials, server-side) ---
RENAISS_INDEX_API_KEY=rk_your_key_here
RENAISS_INDEX_API_SECRET=rsk_your_secret_here

# --- Base URLs (chỉ override nếu Renaiss đổi) ---
RENAISS_MARKETPLACE_BASE=https://api.renaiss.xyz
RENAISS_INDEX_BASE=https://api.renaissos.com

# --- LLM cho Card Passport narration (điền theo provider bạn dùng) ---
# ANTHROPIC_API_KEY=sk-ant-your_key_here
